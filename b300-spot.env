# ComfyuME Serverless - B300 SPOT Configuration
# Usage: ./scripts/switch-gpu.sh b300-spot && docker compose restart queue-manager
#
# GPU: NVIDIA B300 SXM6 288GB HBM3e
# Bandwidth: 8.0 TB/s
# Cost: €1.61/hr + VAT (~€1.92/hr incl. 19% VAT)
# Type: SPOT (can be preempted - but cheap 4K!)
# Best for: 4K experimentation, long video tests, cost-conscious 4K work

INFERENCE_MODE=serverless
SERVERLESS_ACTIVE=b300-spot
SERVERLESS_ENDPOINT_B300_SPOT=https://comfyume-vca-ftv-b300-spot.containers.verda.com

# Fallback
SERVERLESS_ENDPOINT=https://comfyume-vca-ftv-b300-spot.containers.verda.com
