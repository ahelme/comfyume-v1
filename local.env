# ComfyuME Local/Redis Mode Configuration
# Usage: docker compose --env-file local.env up -d queue-manager
#
# Mode: Workers poll Redis queue (local GPU or remote via Tailscale)
# Best for: Development, testing, dedicated GPU instance

INFERENCE_MODE=local
# INFERENCE_MODE=redis  # Use for remote GPU via Tailscale

# Clear serverless settings
SERVERLESS_ACTIVE=default
SERVERLESS_ENDPOINT=
SERVERLESS_ENDPOINT_H200=
SERVERLESS_ENDPOINT_B300=
