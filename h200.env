# ComfyuME Serverless - H200 Configuration
# Usage: docker compose --env-file h200.env up -d queue-manager
# Or: cp h200.env .env.override && docker compose up -d
#
# GPU: NVIDIA H200 SXM5 141GB HBM3e
# Bandwidth: 4.8 TB/s
# Cost: $3.29/hr
# Best for: Workshop (20 users), 720p/1080p video, cost-efficient

INFERENCE_MODE=serverless
SERVERLESS_ACTIVE=h200
SERVERLESS_ENDPOINT_H200=https://comfyui-vca-ftv-h200.containers.verda.com

# Fallback (if SERVERLESS_ACTIVE not working)
SERVERLESS_ENDPOINT=https://comfyui-vca-ftv-h200.containers.verda.com
