# ComfyuME Serverless - B300 Configuration
# Usage: docker compose --env-file b300.env up -d queue-manager
# Or: cp b300.env .env.override && docker compose up -d
#
# GPU: NVIDIA B300 SXM6 288GB HBM3e
# Bandwidth: 8.0 TB/s
# Cost: $4.95/hr
# Best for: 4K production, boss demo, native 4K @ 3+ min video

INFERENCE_MODE=serverless
SERVERLESS_ACTIVE=b300
SERVERLESS_ENDPOINT_B300=https://comfyui-vca-ftv-b300.containers.verda.com

# Fallback (if SERVERLESS_ACTIVE not working)
SERVERLESS_ENDPOINT=https://comfyui-vca-ftv-b300.containers.verda.com
